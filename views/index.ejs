<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>URL Shortner</title>
</head>
<body>

    <div class="container">    
        <h1 class="heading"> URL SHRINKER </h1>
        <div class="container box">
            <form action="/shortUrls" method="POST" class="form">
                <label for="fullUrl" class="visually-hidden"> Url </label>
                <input class="form-control col me-3" required type="url" name="fullUrl" id="fullUrl" placeholder="Url">
                <button type="submit" class="btn style-btn"> Shrink </button>
            </form>
            <table class="table table-striped table-responsive urls">
                <thead class="head">
                    <tr>
                        <th> Full URL </th>
                        <th> Short Url </th>
                        <th> Clicks </th>
                        <th> Action </th>
                    </tr>
                </thead>

                <tbody class="body">
                    <% urls.forEach(url => { %>
                        <tr>
                            <td> <a href="<%= url.full %>"> <%= url.full %> </a> </td>
                            <td> <a href="<%= url.short %>"> <%= url.short %> </a> </td>
                            <td> <%= url.clicks %> </td>
                            <td>
                                <form action="/shortUrls/<%= url._id %>?_method=DELETE" method="POST">
                                    <button type="submit" class="btn style-btn btn-sm"> Delete </button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>        
    </div>
</body>
</html>